<Page x:ConnectionId='1'
    x:Class="MusicPlayerApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MusicPlayerApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                                                           >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>

        <Hub Grid.Row="1" Name="hub" Header="FolderMusic" Style="{StaticResource HHeader}" Margin="0,5,0,0">
            <HubSection Background="Transparent" Margin="0,-10,0,0"
                        Header="{Binding CurrentPlaylistName}" Style="{StaticResource HSHeader}">
                <DataTemplate>
                    <ListBox x:ConnectionId='12' x:Name="lbxCurrentPlaylist" Margin="0,-30,0,0" Background="Transparent" SelectionMode="Single"
                             ItemsSource="{Binding CurrentPlaylistSongs}" 
                             SelectedIndex="{Binding ShuffleListIndex,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                                                                       >
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid x:ConnectionId='13'                                                                          >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <FlyoutBase.AttachedFlyout>
                                        <MenuFlyout>
                                            <MenuFlyoutItem x:ConnectionId='14' Text="Refresh"                          />
                                            <MenuFlyoutItem x:ConnectionId='15' Text="Delete"                         />
                                        </MenuFlyout>
                                    </FlyoutBase.AttachedFlyout>

                                    <StackPanel Orientation="Vertical">
                                        <TextBlock FontSize="20" Margin="0,-5,0,-2"
                                               Text="{Binding Title}" Foreground="{Binding TextBrush}"/>
                                        <TextBlock FontSize="15" Margin="0,-2,0,0" Visibility="{Binding ArtistVisibility}"
                                               Text="{Binding Artist}" Foreground="{Binding TextBrush}" />
                                    </StackPanel>

                                    <Rectangle Grid.Column="2" Fill="Transparent" Width="1500"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DataTemplate>
            </HubSection>

            <HubSection Header="Playlists" Style="{StaticResource HSHeader}" Margin="0,-10,0,0">
                <DataTemplate>
                    <ListBox x:ConnectionId='5' x:Name="lbxPlaylists" SelectionMode="Single" Background="Transparent" Margin="0,-30,0,0"
                             ItemsSource="{Binding Playlists}" SelectedIndex="{Binding CurrentPlaylistIndex,Mode=TwoWay}"
                                                                                 >
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid x:ConnectionId='6'                                    >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <FlyoutBase.AttachedFlyout>
                                        <MenuFlyout>
                                            <MenuFlyoutItem x:ConnectionId='7' Text="Refresh"                              />
                                            <MenuFlyoutItem x:ConnectionId='8' Text="Search for New Songs"                                        />
                                            <MenuFlyoutItem x:ConnectionId='9' Text="Delete"                             />
                                        </MenuFlyout>
                                    </FlyoutBase.AttachedFlyout>

                                    <Image x:ConnectionId='10' Grid.Column="0" Source="{Binding PlayIcon}" 
                                           Width="55" Height="55"                             />

                                    <Grid x:ConnectionId='11' Grid.Column="1"                         >
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" Orientation="Vertical">
                                            <TextBlock Text="{Binding Name}" Foreground="{Binding TextBrush}"
                                                   TextWrapping="WrapWholeWords"
                                                   Margin="10,0,0,0" FontSize="20" VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding SongCount}" Foreground="{Binding TextBrush}"
                                                   Margin="10,0,0,0" FontSize="10" VerticalAlignment="Center"/>
                                        </StackPanel>

                                        <Rectangle Grid.Column="1" Fill="Transparent" Width="1500"/>
                                    </Grid>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DataTemplate>
            </HubSection>
        </Hub>

        <TextBlock Grid.Row="2" Text="{Binding CurrentSongTitle}" FontSize="25" Margin="10,0" TextWrapping="WrapWholeWords"/>
        <TextBlock Grid.Row="3" Text="{Binding CurrentSongArtist}" FontSize="20" Margin="10,0" FontStyle="Italic" 
                   Visibility="{Binding CurrentSongArtistVisibility}"/>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.Resources>
                <Storyboard x:Name="ShuffleImageTap">
                    <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.TranslateY)"
                                                   Storyboard.TargetName="ShuffleImage" AutoReverse="True">
                        <EasingDoubleKeyFrame KeyTime="0" Value="0"/>
                        <EasingDoubleKeyFrame KeyTime="0:0:0.1" Value="-10"/>
                    </DoubleAnimationUsingKeyFrames>
                </Storyboard>

                <Storyboard x:Name="LoopImageTap">
                    <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.TranslateY)" 
                                                   Storyboard.TargetName="LoopImage" AutoReverse="True">
                        <EasingDoubleKeyFrame KeyTime="0" Value="0"/>
                        <EasingDoubleKeyFrame KeyTime="0:0:0.1" Value="-10"/>
                    </DoubleAnimationUsingKeyFrames>
                </Storyboard>
            </Grid.Resources>

            <Slider x:ConnectionId='2' Grid.Column="1" Height="60" VerticalAlignment="Top" Minimum="0" SmallChange="1000" LargeChange="1000000"
                    Value="{Binding CurrentSongPositionMilliseconds,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" 
                    Maximum="{Binding CurrentSongNaturalDurationMilliseconds}"                                    />

            <TextBlock Grid.Column="1" Text="{Binding CurrentSongPostionText}" VerticalAlignment="Bottom"
                       HorizontalAlignment="Left" FontSize="20"/>
            <TextBlock Grid.Column="1" Text="{Binding CurrentSongNaturalDurationText}" VerticalAlignment="Bottom" 
                       HorizontalAlignment="Right" FontSize="20"/>

            <Image x:ConnectionId='3' Grid.Column="0" x:Name="ShuffleImage" Source="{Binding ShuffleIcon}"                        
                                                                                                          >
                <Image.RenderTransform>
                    <CompositeTransform TranslateY="0"/>
                </Image.RenderTransform>
            </Image>

            <Image x:ConnectionId='4' Grid.Column="2" x:Name="LoopImage" Source="{Binding LoopIcon}"                     
                                                                                                    >
                <Image.RenderTransform>
                    <CompositeTransform TranslateY="0"/>
                </Image.RenderTransform>
            </Image>
        </Grid>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.PrimaryCommands>
                <AppBarButton x:ConnectionId='16' Label="Previous" Icon="Previous"                       />
                <AppBarButton x:ConnectionId='17' Label="{Binding PlayPauseText}" Icon="{Binding PlayPauseIcon}"                        />
                <AppBarButton x:ConnectionId='18' Label="Next" Icon="Next"                   />
            </CommandBar.PrimaryCommands>
            
            <CommandBar.SecondaryCommands>
                <AppBarButton x:ConnectionId='19' Label="Refresh every playlists"                                    />
                <AppBarButton x:ConnectionId='20' Label="Search for new playlists"                                    />
                <AppBarButton x:ConnectionId='21' Label="TestFunktion"                           />
                <AppBarButton x:ConnectionId='22' Label="TestFunktion2"                            />
                <AppBarButton x:ConnectionId='23' Label="TestFunktion3"                            />
                <AppBarButton x:ConnectionId='24' Label="TestFunktion4"                            />
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.BottomAppBar>
</Page>

