<UserControl x:ConnectionId='1'
    x:Class="FolderMusic.SongsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FolderMusic"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:con="using:FolderMusic.Converters"
    mc:Ignorable="d"
    d:DesignHeight="800"
    d:DesignWidth="400"
    x:Name="control"
                           >

    <ListBox x:ConnectionId='2' x:Name="lbxSongs" Background="Transparent" SelectionMode="Single"
             DataContext="{Binding ElementName=control,Path=Source}"
                                                         
                                                   >
        <ListBox.ItemTemplate>
            <DataTemplate>
                <Grid x:ConnectionId='3' x:Name="grid"                       >
                    <FlyoutBase.AttachedFlyout>
                        <MenuFlyout>
                            <MenuFlyoutItem x:ConnectionId='4' Text="Refresh"                          />
                            <MenuFlyoutItem x:ConnectionId='5' Text="Remove"                         />
                            <MenuFlyoutItem x:ConnectionId='6' Text="Edit"                       />
                        </MenuFlyout>
                    </FlyoutBase.AttachedFlyout>

                    <Rectangle Fill="Transparent" Width="{Binding ElementName=control,Path=ActualWidth}"/>

                    <StackPanel Orientation="Vertical">
                        <TextBlock FontSize="20" Margin="0,-5,0,-2" Text="{Binding Title}"
                                   Foreground="{ThemeResource AppBarItemForegroundThemeBrush}"/>
                        <TextBlock FontSize="15" Margin="0,-2,0,0" Text="{Binding Artist}"
                                   Foreground="{ThemeResource ApplicationPointerOverForegroundThemeBrush}" />
                    </StackPanel>
                </Grid>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>

